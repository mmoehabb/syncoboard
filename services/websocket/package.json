{
  "name": "websocket",
  "version": "1.0.0",
  "description": "WebSocket service for Syncoboard",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "build": "echo no build command for websocket",
    "lint": "eslint ."
  },
  "dependencies": {
    "@syncoboard/shared": "workspace:*",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "eslint": "8.57.0",
    "eslint-config-next": "^16.2.4",
    "eslint-plugin-unused-imports": "4.4.1"
  }
}
