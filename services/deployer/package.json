{
  "name": "@syncoboard/deployer",
  "version": "1.0.0",
  "description": "Deployment service",
  "main": "index.ts",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun run --watch index.ts",
    "build": "echo no build command for deployer"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  }
}
